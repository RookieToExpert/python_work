dimensions = (200, 50)
print(dimensions[0])

#Writing over a Tuple
dimensions = (400, 100)

#When compared with lists, tuple are simple data strutures. Usd them 
#when you want to store a set of values that should not be changed through-out the lif of a program.